use crate::asset_manager::BrowserItem;
use crate::core::Position;
use crate::engine::EngineType;
pub const DEPTHS: [u32; 8] = [1, 2, 3, 4, 5, 6, 7, 8];
/// Which top-level tab is shown in the lower control area. Splitting the
/// formerly-flat, everything-visible-at-once control panel into tabs
/// keeps any one screen focused on a single task (playing, engine tuning,
/// tournaments, evolution) instead of stacking all of them at once.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum UiPanel {
    Game,
    Engines,
    Tournament,
    Evolution,
}
#[derive(Debug, Clone)]
pub enum Message {
    SquareClicked(Position),
    UndoMove,
    RedoMove,
    ResetBoard,
    GenerateMoves,
    EvaluatePosition,
    EvaluateMoves,
    MakeBestMove,
    WhiteEngineSelected(EngineType),
    BlackEngineSelected(EngineType),
    EvalEngineSelected(EngineType),
    WhiteDepthSelected(u32),
    BlackDepthSelected(u32),
    EvalDepthSelected(u32),
    WhiteTimeInputChanged(String),
    BlackTimeInputChanged(String),
    EvalTimeInputChanged(String),
    WhiteTimeRespectChanged(String),
    BlackTimeRespectChanged(String),
    UnlimitedDepthToggled(bool),
    AutoPlayToggled(bool),
    Tick,
    PlayEngineMove,
    PromotionSelected(usize),
    GameFileSelected(BrowserItem),
    LoadGameFile,
    AnalyzePosition,
    TerminalInputChanged(String),
    TerminalCommand,
    WhiteEngineParameterChanged(String, f64),
    BlackEngineParameterChanged(String, f64),
    EvalEngineParameterChanged(String, f64),
    PrintPgn,
    LoadPgn,
    TournamentStart,
    TournamentStop,
    TournamentClearHistory,
    TournamentToggleParticipant(EngineType),
    TournamentGamesInputChanged(String),
    TournamentMaxPliesInputChanged(String),
    TournamentParallelismInputChanged(String),
    TournamentTick,
    EvolutionBaseEngineSelected(EngineType),
    EvolutionPopulationChanged(String),
    EvolutionPlayBiasChanged(String),
    EvolutionReplicationBiasChanged(String),
    EvolutionMutationScaleChanged(String),
    EvolutionReproRateChanged(String),
    EvolutionCrossoverToggled(bool),
    EvolutionMaxPliesInputChanged(String),
    EvolutionParallelismInputChanged(String),
    EvolutionAutosaveToggled(bool),
    EvolutionAutosavePathChanged(String),
    EvolutionParamLockToggled(String, bool),
    ToggleEvolutionLockMenu,
    EvolutionExportBest,
    EvolutionStart,
    EvolutionStop,
    EvolutionTick,
    UiPanelSelected(UiPanel),
    ToggleConsole,
}
